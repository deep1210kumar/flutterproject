name: doctor
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  vm_service: 14.2.1

  # Downgraded to match flutter_test dependency

dev_dependencies:
  flutter_test:
    sdk: flutter

  # other dev dependencies

flutter:
  uses-material-design: true

  assets:
   - assets/
